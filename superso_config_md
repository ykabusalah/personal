## Features

- ✅ Random background drawings on home page only
- ✅ Drawing history prevention (last 5 drawings won't repeat)
- ✅ Persistent history across page loads using sessionStorage
- ✅ Google Analytics with cross-domain tracking
- ✅ Enhanced device and interaction tracking
- ✅ Responsive design with screen size detection
- ✅ Mobile-friendly message for small screens
- ✅ Dynamic email positioning (center on desktop, left corner on mobile/half-view)
- ✅ "Art by:" attribution for drawings
- ✅ Secure RLS-based data access
- ✅ Transparent navbar
- ✅ Chat widget integration
- ✅ Custom domain support (ykabusalah.me)### Global Head
```html
<script 
  src="https://cdnjs.cloudflare.com/ajax/libs/bodymovin/5.7.8/lottie_svg.min.js" 
  type="text/javascript">
</script>
<script 
  src="https://api.minymon.com/minymon.js" 
  defer
  type="text/javascript">
</script>

<!-- Global site tag (gtag.js) - Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-DM7G0WM5ZM"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){ dataLayer.push(arguments); }
  gtag('js', new Date());
  gtag('config', 'G-DM7G0WM5ZM', {
    linker: {
      domains: [
        'ykabusalah.me',
        'draw.ykabusalah.me'
      ]
    }
  });

  // Enhanced device and interaction tracking
  (function() {
    // Track page view with enhanced device info
    gtag('event', 'page_view_enhanced', {
      event_category: 'device_tracking',
      device_type: /Mobi|Android/i.test(navigator.userAgent) ? 'mobile' : 'desktop',
      screen_width: window.innerWidth,
      screen_height: window.innerHeight,
      user_agent_short: navigator.userAgent.substring(0, 100),
      viewport_ratio: Math.round((window.innerWidth / window.innerHeight) * 100) / 100
    });

    // Track when someone navigates to draw app
    document.addEventListener('click', function(e) {
      const target = e.target;
      const closestLink = target.closest('a');
      
      if (closestLink) {
        const href = closestLink.getAttribute('href') || '';
        
        // Check if it's a link to the drawing app
        if (href.includes('draw.ykabusalah.me') || href.includes('draw') || 
            closestLink.textContent.toLowerCase().includes('draw')) {
          
          gtag('event', 'navigate_to_draw_app', {
            event_category: 'navigation',
            event_label: 'main_to_draw_transition',
            link_text: closestLink.textContent.trim().substring(0, 50),
            link_url: href
          });
        }
      }
    });

    // Track button interactions on main site
    document.addEventListener('click', function(e) {
      const target = e.target;
      
      // Track any button clicks
      if (target.tagName === 'BUTTON' || target.closest('button')) {
        const button = target.tagName === 'BUTTON' ? target : target.closest('button');
        
        gtag('event', 'button_click', {
          event_category: 'engagement',
          event_label: button.textContent.trim().substring(0, 30) || 'unnamed_button'
        });
      }
    });
  })();
</script>
```